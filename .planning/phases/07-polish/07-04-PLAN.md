---
phase: 07-polish
plan: 04
type: execute
wave: 3
depends_on: [07-02, 07-03]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/components/Toolbar.tsx
autonomous: true

must_haves:
  truths:
    - App.tsx integrates all new components
    - Toolbar with action buttons (settings, saved searches, review queue, export)
    - JobProgress rendered globally (always visible)
    - ReviewQueue accessible via toolbar button
    - SettingsPanel opens from toolbar
    - SavedSearches dropdown in header
    - ExportButton in toolbar
    - FavoriteButton on ResultRow
    - Build succeeds and app is fully wired
  artifacts:
    - frontend/src/App.tsx (final version)
    - frontend/src/components/Toolbar.tsx
  key_links:
    - App.tsx manages panel state (settings, review, saved searches)
    - Toolbar groups action buttons
    - JobProgress is always-on overlay
---

<objective>
Wire all Phase 7 components into the app layout. Create toolbar, integrate panels, finalize the v1 UI.

Purpose: Complete the v1 app — all features accessible, fully integrated.
Output: Final App.tsx with toolbar, all panels wired, JobProgress overlay.
</objective>

<execution_context>
@skills/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/components/SearchBar.tsx
@frontend/src/components/DocumentDetail.tsx
@frontend/src/components/ResultRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toolbar component</name>
  <files>frontend/src/components/Toolbar.tsx</files>
  <action>
  Create `frontend/src/components/Toolbar.tsx`:

  Named export `Toolbar`.
  Props:
  - onOpenSettings: () => void
  - onOpenReviewQueue: () => void
  - onOpenSavedSearches: () => void
  - query: string
  - filters: SearchFilters
  - resultCount?: number

  Renders a horizontal bar with action buttons:
  1. Saved searches button (Bookmark icon) — triggers onOpenSavedSearches
  2. Review queue button (ClipboardCheck icon) — triggers onOpenReviewQueue
  3. Export button (inline ExportButton component)
  4. Settings button (Settings icon) — triggers onOpenSettings

  Style:
  - flex items-center gap-2, px-4 py-2
  - border-b border-zinc-800
  - Buttons: ghost variant, icon + text, text-zinc-400
  - Right-aligned (ml-auto or justify-end)
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  Toolbar with settings, review, saved searches, and export buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final App.tsx integration</name>
  <files>frontend/src/App.tsx, frontend/src/components/ResultRow.tsx</files>
  <action>
  Rewrite `frontend/src/App.tsx` to integrate everything:

  State additions:
  - showSettings: boolean (false)
  - showReviewQueue: boolean (false)
  - showSavedSearches: boolean (false)

  Layout (top to bottom):
  1. **Header**: SearchBar (existing) + Toolbar row below it
  2. **Main content**: 3-column layout (existing)
     - Left: FilterSidebar
     - Center: FilterChips + ResultsList (or ReviewQueue when active)
     - Right: DocumentDetail (when doc selected)
  3. **Overlays/modals**:
     - SettingsPanel (modal, conditional on showSettings)
     - SavedSearches (dropdown/popover, conditional)
  4. **Fixed overlay**: JobProgress (always rendered, bottom-right)

  SavedSearches integration:
  - onLoadSearch: parse JSON, set search.setQuery + search.setFilters

  ReviewQueue integration:
  - When showReviewQueue is true, center column shows ReviewQueue instead of ResultsList
  - ReviewQueue onSelectDoc opens document detail

  Update ResultRow to include FavoriteButton:
  - Add FavoriteButton as last element in each result row
  - Pass doc.id to FavoriteButton

  Import all new components.
  </action>
  <verify>
  `cd frontend && npm run build 2>&1 | tail -5` — build must succeed
  </verify>
  <done>
  App.tsx fully integrated: toolbar, settings modal, review queue, saved searches, job progress, favorites on results. Build succeeds.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run build
```
Build must succeed. All features accessible in the UI.
</verification>

<success_criteria>
- Toolbar visible with all action buttons
- Settings opens modal, saves config
- Review queue replaces results when active
- Saved searches save/load/delete
- Job progress always visible when jobs active
- Favorites toggleable on result rows
- CSV export works from toolbar
- Build succeeds
- Milestone M3 complete (v1 ready to use)
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-04-SUMMARY.md`
</output>
