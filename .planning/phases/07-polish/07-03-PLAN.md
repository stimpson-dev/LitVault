---
phase: 07-polish
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/components/JobProgress.tsx
  - frontend/src/components/SavedSearches.tsx
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/ExportButton.tsx
autonomous: true

must_haves:
  truths:
    - Job progress shown in UI via SSE consumer
    - Saved searches UI: save current search, load saved, delete
    - Settings panel: edit watch folders, Ollama URL, model
    - Export button triggers CSV download
    - All components use existing API functions
  artifacts:
    - frontend/src/components/JobProgress.tsx
    - frontend/src/components/SavedSearches.tsx
    - frontend/src/components/SettingsPanel.tsx
    - frontend/src/components/ExportButton.tsx
  key_links:
    - JobProgress uses EventSource for SSE from /api/jobs/{id}/progress
    - SavedSearches uses saved-searches API
    - SettingsPanel uses settings API
    - ExportButton generates download URL for /api/search/export
---

<objective>
Build job progress display, saved searches UI, settings panel, and CSV export button.

Purpose: Complete the v1 feature set — users can monitor jobs, save searches, configure settings, export results.
Output: JobProgress, SavedSearches, SettingsPanel, ExportButton components.
</objective>

<execution_context>
@skills/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: JobProgress component (SSE consumer)</name>
  <files>frontend/src/components/JobProgress.tsx</files>
  <action>
  Create `frontend/src/components/JobProgress.tsx`:

  Named export `JobProgress`.
  Props: none (self-contained, fetches own data)

  State:
  - jobs: Job[] (fetched from listJobs)
  - activeJobId: string | null (currently streaming)
  - progress: JobProgress | null (from SSE)

  Behavior:
  1. On mount: fetch listJobs() to get recent jobs
  2. Poll listJobs() every 5 seconds to detect new jobs
  3. If any job has status "processing", connect EventSource to /api/jobs/{id}/progress
  4. Parse SSE data events, update progress state
  5. On job completion (status "done" or "error"), close EventSource, refresh job list

  Display:
  - Small floating indicator at bottom-right of screen (fixed position)
  - When no active jobs: hidden or show small icon
  - When job active: show progress bar + message
    - Progress bar: w-64, zinc-800 bg, green-500 fill, rounded
    - Text: "{current}/{total} — {message}"
    - Status badge: processing=yellow, done=green, error=red
  - Click expands to show recent job history (last 5 jobs)
  - Close/collapse button

  Use EventSource API directly (browser native).
  Clean up EventSource on unmount (useEffect cleanup).

  Style:
  - Fixed bottom-right: fixed bottom-4 right-4 z-50
  - Panel: zinc-900 bg, border border-zinc-800, rounded-lg, shadow-xl
  - Compact by default, expandable on click
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  JobProgress: SSE consumer, progress bar, job history, fixed bottom-right indicator.
  </done>
</task>

<task type="auto">
  <name>Task 2: SavedSearches component</name>
  <files>frontend/src/components/SavedSearches.tsx</files>
  <action>
  Create `frontend/src/components/SavedSearches.tsx`:

  Named export `SavedSearches`.
  Props:
  - currentQuery: string
  - currentFilters: SearchFilters
  - onLoadSearch: (query: string, filters: SearchFilters) => void
  - onClose: () => void

  State:
  - searches: SavedSearch[] (fetched on mount)
  - saveName: string (input for new save)
  - saving: boolean

  Features:
  1. List saved searches with name + created_at
  2. Click a saved search: parse its query JSON, call onLoadSearch with extracted query + filters
  3. Delete button (X) per saved search with confirmation
  4. Save current search:
     - Input for name
     - "Speichern" button
     - Serializes current query + filters to JSON string
     - Calls createSavedSearch(name, jsonString)
     - Refreshes list
  5. Empty state: "Keine gespeicherten Suchen"

  The `query` field in SavedSearch stores a JSON string like:
  `{"q": "gear stress", "category": "FEM", "doc_type": "paper"}`

  On load: parse JSON, extract `q` as search query, rest as filters.

  Style:
  - Dropdown/popover panel below a trigger button
  - Or: slide-in panel similar to settings
  - zinc-900 bg, list items with hover, border-b separators
  - Save input at top with button
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  SavedSearches: list, load, save current, delete. JSON serialization of query+filters.
  </done>
</task>

<task type="auto">
  <name>Task 3: SettingsPanel component</name>
  <files>frontend/src/components/SettingsPanel.tsx</files>
  <action>
  Create `frontend/src/components/SettingsPanel.tsx`:

  Named export `SettingsPanel`.
  Props:
  - onClose: () => void

  State:
  - settings: AppSettings | null (fetched via getSettings on mount)
  - draft: Partial<AppSettings> (edited values)
  - saving: boolean
  - saved: boolean (flash "Gespeichert!" message)

  Features:
  1. Fetch current settings on mount
  2. Form fields:
     - **Watch Folders**: list of text inputs, add/remove buttons. Each folder as a text input.
     - **Ollama URL**: text input (e.g., http://192.168.178.43:30068)
     - **Ollama Model**: text input (e.g., qwen3:8b)
     - **Poll Interval**: number input (seconds)
  3. "Speichern" button: calls updateSettings(draft), shows success flash
  4. "Abbrechen" button: calls onClose

  Style:
  - Full-page overlay or modal: fixed inset-0, bg-black/50 backdrop
  - Centered panel: max-w-lg, zinc-900 bg, rounded-lg, p-6
  - Form labels: text-sm text-zinc-400
  - Inputs: zinc-800 bg, zinc-100 text, rounded
  - Heading: "Einstellungen"
  - Save button: primary color, cancel: ghost
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  SettingsPanel: modal with watch folders, Ollama config, poll interval. Save to backend.
  </done>
</task>

<task type="auto">
  <name>Task 4: ExportButton component</name>
  <files>frontend/src/components/ExportButton.tsx</files>
  <action>
  Create `frontend/src/components/ExportButton.tsx`:

  Named export `ExportButton`.
  Props:
  - query: string
  - filters: SearchFilters
  - disabled?: boolean

  Behavior:
  - On click: construct the export URL via getExportUrl(query, filters)
  - Open URL in new tab (window.open) or create a temporary <a> element with download attribute
  - The URL points to /api/search/export?q=...&category=...&etc

  Render:
  - Button with Download icon (lucide-react Download)
  - Text: "CSV Export"
  - Style: ghost variant, text-zinc-400 hover:text-zinc-200
  - Disabled when no results / disabled prop
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  ExportButton: triggers CSV download via backend export endpoint.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run build
```
Build must succeed.
</verification>

<success_criteria>
- JobProgress: SSE consumer with progress bar, job history
- SavedSearches: save/load/delete with JSON serialization
- SettingsPanel: edit watch folders + Ollama config, persists
- ExportButton: CSV download trigger
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-03-SUMMARY.md`
</output>
