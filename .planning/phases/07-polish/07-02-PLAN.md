---
phase: 07-polish
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - frontend/src/components/DocumentDetail.tsx
  - frontend/src/components/TagEditor.tsx
  - frontend/src/components/ReviewQueue.tsx
  - frontend/src/components/FavoriteButton.tsx
autonomous: true

must_haves:
  truths:
    - DocumentDetail shows tags with add/remove capability
    - Favorite toggle button on DocumentDetail and ResultRow
    - Tag editor: inline input to add tags, X to remove, AI vs user source shown
    - Review queue page: lists docs with classification_confidence < 0.85
    - Review queue allows approving (set source=user) or editing classification
    - User can edit document metadata inline (title, authors, year, etc.)
  artifacts:
    - frontend/src/components/TagEditor.tsx
    - frontend/src/components/ReviewQueue.tsx
    - frontend/src/components/FavoriteButton.tsx
    - frontend/src/components/DocumentDetail.tsx (extended)
  key_links:
    - TagEditor uses addDocumentTag / removeDocumentTag API
    - FavoriteButton uses toggleFavorite API
    - ReviewQueue fetches docs with needs-review status via search API
    - Inline edit uses updateDocument API
---

<objective>
Build tagging workflow, favorites button, review queue, and inline document editing.

Purpose: Users can correct AI classifications, manage tags, favorite documents, and review low-confidence items.
Output: TagEditor, FavoriteButton, ReviewQueue components. Enhanced DocumentDetail with editing.
</objective>

<execution_context>
@skills/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-frontend/06-SUMMARY.md
@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
@frontend/src/components/DocumentDetail.tsx
@frontend/src/components/ResultRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: FavoriteButton component</name>
  <files>frontend/src/components/FavoriteButton.tsx</files>
  <action>
  Create `frontend/src/components/FavoriteButton.tsx`:

  Named export `FavoriteButton`.
  Props:
  - docId: number
  - favorited?: boolean (initial state, optional)
  - onToggle?: (favorited: boolean) => void

  State:
  - isFavorited: boolean (initialized from prop)
  - loading: boolean

  On click: call toggleFavorite(docId), update state, call onToggle callback.

  Render: Heart icon (lucide-react Heart). Filled when favorited (fill-red-500 text-red-500), outline when not.
  Style: p-1 rounded hover:bg-zinc-800 cursor-pointer, transition colors.
  Disable click while loading.
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  FavoriteButton: toggle heart icon, calls API, updates state.
  </done>
</task>

<task type="auto">
  <name>Task 2: TagEditor component</name>
  <files>frontend/src/components/TagEditor.tsx</files>
  <action>
  Create `frontend/src/components/TagEditor.tsx`:

  Named export `TagEditor`.
  Props:
  - docId: number

  State:
  - tags: TagItem[] (fetched via getDocumentTags on mount/docId change)
  - newTag: string (input value)
  - loading: boolean

  Features:
  1. Fetch tags on mount and docId change
  2. Display tags as pills/badges:
     - AI-sourced tags: zinc-700 bg, small "AI" indicator
     - User-sourced tags: blue-700 bg
     - Each tag has X button to remove (calls removeDocumentTag)
  3. Input field at bottom: type tag name, press Enter to add
     - Calls addDocumentTag(docId, name)
     - Clears input on success
     - Prevent empty/duplicate submissions
  4. Loading state while fetching

  Style:
  - flex flex-wrap gap-1.5 for tag pills
  - Input: small, zinc-800 bg, rounded, placeholder "Tag hinzufügen..."
  - Tags: text-xs px-2 py-1 rounded-full
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -5` — no errors
  </verify>
  <done>
  TagEditor: shows tags per document, add via input, remove via X. AI vs user source distinction.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhanced DocumentDetail with tags, favorites, inline edit</name>
  <files>frontend/src/components/DocumentDetail.tsx</files>
  <action>
  Extend `frontend/src/components/DocumentDetail.tsx`:

  1. Add FavoriteButton in header (next to close button)
  2. Add TagEditor section below metadata grid
  3. Add inline editing capability:
     - Each metadata field gets a small edit icon (Pencil from lucide)
     - Clicking edit toggles field to input/select mode
     - On blur or Enter: call updateDocument(docId, { field: newValue })
     - On success: update local doc state
     - Editable fields: title, authors (as text), year, doc_type (select), source, language, summary (textarea)
  4. Show classification_source badge: "AI" (zinc) or "Benutzer" (blue)

  Keep the existing metadata grid structure. Add edit toggle per row.
  Use a simple state approach: `editingField: string | null` to track which field is being edited.

  Import FavoriteButton and TagEditor.
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -10` — no errors
  </verify>
  <done>
  DocumentDetail with favorites, tags, inline metadata editing, classification source display.
  </done>
</task>

<task type="auto">
  <name>Task 4: ReviewQueue component</name>
  <files>frontend/src/components/ReviewQueue.tsx</files>
  <action>
  Create `frontend/src/components/ReviewQueue.tsx`:

  Named export `ReviewQueue`.
  Props:
  - onSelectDoc: (docId: number) => void (to open detail panel)
  - onClose: () => void

  State:
  - documents: SearchDocument[] (fetched)
  - loading: boolean

  Fetch: GET /api/search with no query but a special approach:
  - Use searchDocuments('', {}, 0, 100) and filter client-side where classification_confidence < 0.85 and classification_confidence !== null
  - OR: add a query param to the search API for confidence filtering (simpler: just filter client-side for now)

  Note: A better approach is to add a GET /api/documents/review endpoint to the backend that queries WHERE classification_confidence < 0.85 AND classification_confidence IS NOT NULL. But for now, use the list documents endpoint with client-side filtering — it's simpler and this is v1.

  Actually, use `GET /api/documents?limit=200` and filter client-side. The list endpoint returns processing_status and we can check docs where classification needs review.

  Display:
  - Full-width overlay/panel (replaces center content when active)
  - Heading: "Überprüfung" with count badge
  - List of documents needing review:
    - Title (or filename), confidence percentage, doc_type, category
    - Click opens document in detail panel (onSelectDoc)
    - "Bestätigen" button: calls updateDocument(docId, { doc_type: doc.doc_type }) with source="user" to confirm AI classification
  - Empty state: "Keine Dokumente zur Überprüfung"
  - Close button

  Style: zinc-900 bg panel, consistent with app theme.
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -10` — no errors
  </verify>
  <done>
  ReviewQueue: lists docs needing classification review, confirm/edit actions, navigates to detail.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run build
```
Build must succeed.
</verification>

<success_criteria>
- FavoriteButton toggles heart icon + calls API
- TagEditor shows, adds, removes tags with AI/user distinction
- DocumentDetail has favorites, tags, inline edit
- ReviewQueue lists low-confidence documents with confirm action
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-02-SUMMARY.md`
</output>
