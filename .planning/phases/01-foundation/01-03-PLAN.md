---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/database.py
  - backend/app/documents/models.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/001_initial_schema.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Database file is created when app starts"
    - "All tables exist: documents, tags, categories, document_tags, document_categories, saved_searches, favorites, embeddings"
    - "FTS5 virtual table exists with triggers for INSERT/UPDATE/DELETE"
    - "WAL mode is active"
    - "Alembic migration runs without errors"
  artifacts:
    - backend/app/database.py
    - backend/app/documents/models.py
    - backend/alembic.ini
    - backend/alembic/env.py
  key_links:
    - "SQLAlchemy event listener sets WAL mode on every connection"
    - "FTS5 triggers keep index in sync with documents table"
    - "Alembic env.py includes include_object to skip FTS virtual tables"
---

<objective>
Create the complete database layer: SQLAlchemy async engine, all ORM models, FTS5 virtual table with sync triggers, and Alembic migration setup.

Purpose: The database is the backbone of LitVault. Every other component reads from or writes to it.
Output: Working database with all tables, FTS5 index, and migration infrastructure.
</objective>

<context>
@.planning/PROJECT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database engine and SQLAlchemy models</name>
  <files>
    backend/app/database.py
    backend/app/documents/models.py
  </files>
  <action>
  **backend/app/database.py:**
  1. Create async engine with `sqlite+aiosqlite:///./litvault.db`
  2. Set `connect_args={"check_same_thread": False}`
  3. Create async_sessionmaker (expire_on_commit=False)
  4. Add SQLAlchemy event listener on sync_engine "connect" to set:
     - PRAGMA journal_mode=WAL
     - PRAGMA synchronous=NORMAL
     - PRAGMA foreign_keys=ON
     - PRAGMA cache_size=-64000
     - PRAGMA busy_timeout=30000
  5. Create `get_db()` async generator for FastAPI dependency injection
  6. Create declarative Base

  **backend/app/documents/models.py:**
  Define all SQLAlchemy models matching the schema from research:

  - **Document**: id, file_path (unique), file_hash, file_type, file_size, mtime, title, authors (JSON string), year, doc_type, source, language, summary, full_text, has_text, doi, processing_status (enum: pending/processing/done/error), classification_confidence, classification_source, created_at, updated_at, indexed_at
  - **Tag**: id, name (unique)
  - **Category**: id, name (unique)
  - **DocumentTag**: document_id (FK), tag_id (FK), source (ai/user/rule), composite PK
  - **DocumentCategory**: document_id (FK), category_id (FK), source, composite PK
  - **SavedSearch**: id, name (unique), query (JSON), created_at
  - **Favorite**: document_id (PK, FK), added_at
  - **Embedding**: document_id (PK, FK), model, vector (LargeBinary), created_at

  Use proper relationship() definitions for ORM navigation.
  Use `datetime('now')` as server_default for timestamps.
  </action>
  <verify>
  Run: `cd backend && uv run python -c "from app.documents.models import Document, Tag, Category; print('Models OK')"` — must print "Models OK"
  </verify>
  <done>All 8 models defined with relationships, database.py creates engine with WAL pragmas</done>
</task>

<task type="auto">
  <name>Task 2: Set up Alembic with FTS5 migration</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/versions/001_initial_schema.py
  </files>
  <action>
  1. Run `cd backend && uv run alembic init alembic`
  2. Edit alembic.ini: set sqlalchemy.url = sqlite:///./litvault.db
  3. Edit alembic/env.py:
     - Import Base from app.database and all models from app.documents.models
     - Set target_metadata = Base.metadata
     - Add `include_object` function that returns False for tables ending in "_fts"
     - Configure `render_as_batch=True` in run_migrations_online context
     - Use synchronous engine (not async) for migrations
  4. Create manual migration 001_initial_schema.py:
     - Use `op.create_table()` for all regular tables (via autogenerate or manual)
     - Add `op.execute()` for FTS5 virtual table:
       ```sql
       CREATE VIRTUAL TABLE IF NOT EXISTS documents_fts USING fts5(
           title, authors, full_text, summary,
           content=documents, content_rowid=id,
           tokenize='porter unicode61 remove_diacritics 1'
       );
       ```
     - Add `op.execute()` for the 3 FTS sync triggers (INSERT, UPDATE, DELETE)
     - Seed initial categories: Verzahnungsgrundlagen, Kegelrad/Hypoid/Stirnrad, Tragbild/Kontakt/NVH, FEM/Spannungen/Lebensdauer, Werkstoffe/Wärmebehandlung, Fertigung/Schleifen/Honen/Härten, Prüfstand/Versuch/Schadensanalyse, Normen/ISO/DIN/AGMA/FVA, Anwendungen/Differential/E-Achse/Nutzfahrzeug, Interne Berichte/Projektdokumente
  5. Run `cd backend && uv run alembic upgrade head` to apply migration
  </action>
  <verify>
  Run: `cd backend && uv run alembic upgrade head` — must complete without errors.
  Run: `cd backend && uv run python -c "
import sqlite3
conn = sqlite3.connect('litvault.db')
tables = [r[0] for r in conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()]
print('Tables:', sorted(tables))
assert 'documents' in tables
assert 'documents_fts' in tables
assert 'categories' in tables
cats = conn.execute('SELECT name FROM categories').fetchall()
print('Categories:', len(cats))
assert len(cats) == 10
conn.close()
print('DB OK')
"` — must print tables list, 10 categories, and "DB OK"
  </verify>
  <done>Alembic configured, migration creates all tables + FTS5 + triggers + seed categories, migration runs clean</done>
</task>

</tasks>

<verification>
1. `uv run alembic upgrade head` completes without errors
2. SQLite DB file exists with all tables
3. FTS5 virtual table exists
4. 3 triggers exist (documents_ai, documents_au, documents_ad)
5. 10 seed categories exist
6. WAL mode is set (check PRAGMA journal_mode returns 'wal')
</verification>

<success_criteria>
- All 8 tables + FTS5 virtual table exist in SQLite
- Triggers keep FTS5 in sync
- WAL mode active on every connection
- Alembic tracks migration state
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
