---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/app/config.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Config loads from config.json at project root"
    - "Missing config.json falls back to defaults"
    - "Config values accessible via Settings singleton"
  artifacts:
    - backend/app/config.py
  key_links:
    - "CONFIG_PATH resolves relative to project root (not backend/)"
---

<objective>
Create the Pydantic Settings configuration system that loads from config.json.

Purpose: Centralized config management for all backend components (DB path, Ollama URL, watch folders).
Output: Settings class with JSON file source and sensible defaults.
</objective>

<context>
@.planning/PROJECT.md
@.planning/research/STACK.md
@config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pydantic Settings config module</name>
  <files>backend/app/config.py</files>
  <action>
  Create backend/app/config.py with:

  1. Import pydantic_settings BaseSettings
  2. Resolve CONFIG_PATH to project root (backend/../config.json)
  3. Define Settings class with fields:
     - watch_folders: list[str] = []
     - ollama_url: str = "http://localhost:11434"
     - ollama_model: str = "qwen3:8b"
     - embedding_model: str = "nomic-ai/nomic-embed-text-v1.5"
     - db_path: str = "litvault.db" (resolve relative to backend/)
     - thumbnails_dir: str = "thumbnails"
     - log_level: str = "INFO"
     - poll_interval_seconds: int = 10
  4. Configure model_config to load from JSON file
  5. Create a `get_settings()` function that returns a cached Settings instance
  6. Handle missing config.json gracefully (use defaults)
  </action>
  <verify>
  Run: `cd backend && uv run python -c "from app.config import get_settings; s = get_settings(); print(s.ollama_url)"` — must print "http://localhost:11434"
  </verify>
  <done>Settings loads from config.json, all fields have defaults, missing file handled</done>
</task>

</tasks>

<verification>
1. Settings loads successfully from config.json
2. Changing a value in config.json is reflected in Settings
3. Missing config.json → defaults used without crash
</verification>

<success_criteria>
- `get_settings()` returns valid Settings object
- All config values accessible as typed attributes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
