---
phase: 10-document-list
plan: 01
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - frontend/src/components/documents/DocumentToolbar.tsx
  - frontend/src/components/documents/DisplayModeToggle.tsx
  - frontend/src/components/documents/SortControls.tsx
  - frontend/src/components/documents/LargeCard.tsx
  - frontend/src/components/ResultsList.tsx
  - frontend/src/components/GridCard.tsx
  - frontend/src/components/ResultRow.tsx
  - frontend/src/lib/types.ts
autonomous: true

must_haves:
  truths:
    - User can switch between 3 display modes: table, small cards, large cards
    - Display mode preference persists across sessions
    - Large card shows thumbnail left + metadata right with content snippet
    - Sort controls allow choosing field and direction in the toolbar
    - Hover over a card reveals action buttons (open, download) and selection checkbox
  artifacts:
    - frontend/src/components/documents/DocumentToolbar.tsx
    - frontend/src/components/documents/DisplayModeToggle.tsx
    - frontend/src/components/documents/LargeCard.tsx
  key_links:
    - DisplayModeToggle ↔ settings.view_mode (now 'table' | 'grid' | 'large')
    - SortControls ↔ search.sort (triggers re-search on change)
    - LargeCard ↔ SearchDocument type (renders all metadata)
    - Hover actions ↔ group/group-hover Tailwind pattern
---

<objective>
Upgrade the document list with 3 display modes (table / small cards / large cards), a toolbar with sort controls and display mode toggle, and hover-reveal card actions.

Purpose: Matches paperless-ngx's flexible document viewing — power users get dense tables, visual users get rich cards.
Output: DocumentToolbar, DisplayModeToggle, SortControls, LargeCard components.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/components/ResultsList.tsx
@frontend/src/components/ResultRow.tsx
@frontend/src/components/GridCard.tsx
@frontend/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentToolbar + DisplayModeToggle + SortControls</name>
  <files>
    frontend/src/components/documents/DocumentToolbar.tsx
    frontend/src/components/documents/DisplayModeToggle.tsx
    frontend/src/components/documents/SortControls.tsx
    frontend/src/lib/types.ts
  </files>
  <action>
    **Types**: Extend view_mode from 'list' | 'grid' to 'table' | 'grid' | 'large'.
    Update AppSettings type and useSettings accordingly.

    **DisplayModeToggle**: Three-segment toggle group:
    - Table icon (TableProperties or List icon)
    - Small cards icon (LayoutGrid icon)
    - Large cards icon (Rows3 or LayoutList icon)
    - Active segment highlighted with accent background
    - On change: update settings.view_mode + persist to backend

    **SortControls**: Inline in toolbar:
    - Dropdown select for sort field: relevance, date_desc, date_asc, name_asc, name_desc, year
    - Toggle button for asc/desc (ArrowUpDown icon rotates)
    - On change: update search sort and trigger re-search

    **DocumentToolbar**: Horizontal bar above results:
    - Left: Selection controls (when items selected: count badge, "Select all" / "Deselect" / "Exclude selected")
    - Center: (spacer)
    - Right: SortControls + DisplayModeToggle

    Replace current selection controls in ResultsList with this toolbar.
  </action>
  <verify>
    npm run build succeeds.
    Switching display mode changes the view.
    Changing sort re-fetches results in new order.
  </verify>
  <done>
    Toolbar renders with display mode toggle and sort controls.
    Switching modes persists to settings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Large card + hover-reveal actions</name>
  <files>
    frontend/src/components/documents/LargeCard.tsx
    frontend/src/components/ResultsList.tsx
    frontend/src/components/GridCard.tsx
    frontend/src/components/ResultRow.tsx
  </files>
  <action>
    **LargeCard** (new component — two-column layout):
    - Left (w-24 or w-32): Thumbnail image (object-fit cover, object-position top left).
      If no thumbnail: file type icon placeholder (FileText, File, Presentation).
    - Right (flex-1):
      - Header: doc_type badge + title (truncated 2 lines) + favorite heart
      - Authors (if present) + year
      - Content snippet (3 lines max, from search snippet or summary)
      - Tags row (colored pills, max 5 shown + "+N more")
      - Footer: file_type icon, file_size, page_count, confidence bar, created date
    - Hover-reveal actions (absolute positioned, top-right):
      - Selection checkbox (appears on hover via group-hover)
      - Open file button (ExternalLink icon)
      - Download button (Download icon)
      - "More like this" button (Search icon) — adds moreLike filter

    **Hover pattern** (apply to all card types):
    ```tsx
    <div className="group relative">
      <div className="hidden group-hover:flex absolute top-2 right-2 gap-1 z-10">
        <Button size="icon" variant="secondary" className="h-7 w-7">...</Button>
      </div>
      {/* card content */}
    </div>
    ```

    **Search relevance score** (on all cards when search is active):
    - Small colored bar (h-1, w-16) in the footer
    - Color: green (score > 0.7), yellow (> 0.3), red (≤ 0.3)
    - Width proportional to score (0-100%)

    **ResultsList**: Update to render based on view_mode:
    - 'table': existing ResultRow-based list (keep as-is)
    - 'grid': existing GridCard (small cards, keep as-is, add hover actions)
    - 'large': new LargeCard in a vertical stack (gap-3)

    Add hover-reveal actions to existing GridCard too.
  </action>
  <verify>
    Large cards render with thumbnail + metadata + actions.
    Hovering a card reveals action buttons.
    Relevance bar shows on search results.
    All 3 display modes render correctly.
  </verify>
  <done>
    Three display modes fully functional.
    Hover-reveal actions on all card types.
    Relevance score visualization shows on search results.
  </done>
</task>

</tasks>

<verification>
- [ ] Display mode toggle switches between table / grid / large
- [ ] Large card renders: thumbnail, title, authors, tags, snippet, footer metadata
- [ ] Hover over any card reveals checkbox + action buttons
- [ ] Sort controls change result order
- [ ] Relevance bar shows green/yellow/red based on score
- [ ] Display mode persists to settings
- [ ] Selection controls work in toolbar
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Three display modes render correctly with all document metadata.
Sort controls and display mode toggle work in toolbar.
Hover-reveal actions appear on all card types.
</success_criteria>

<output>
After completion, create `.planning/phases/10-document-list/10-01-SUMMARY.md`
</output>
