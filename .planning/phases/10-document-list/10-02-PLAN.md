---
phase: 10-document-list
plan: 02
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - frontend/src/components/filters/FilterBar.tsx
  - frontend/src/components/filters/FilterDropdown.tsx
  - frontend/src/components/filters/FilterSelectionModel.ts
  - frontend/src/components/filters/DateRangeFilter.tsx
  - frontend/src/components/FilterSidebar.tsx
  - frontend/src/components/FilterChips.tsx
autonomous: true

must_haves:
  truths:
    - Filters appear as a sticky horizontal bar of dropdown buttons below the toolbar
    - Each dropdown shows a searchable list of options with document counts
    - Clicking an option toggles it as included, second click excludes, third click clears
    - Active filters show count badges on their dropdown buttons
    - "Reset filters" button clears all active filters
    - Filter state is reflected in URL query params
  artifacts:
    - frontend/src/components/filters/FilterBar.tsx
    - frontend/src/components/filters/FilterDropdown.tsx
    - frontend/src/components/filters/FilterSelectionModel.ts
  key_links:
    - FilterDropdown ↔ SearchFacets (renders options with counts)
    - FilterSelectionModel ↔ SearchFilters (translates selections to API params)
    - FilterBar ↔ URL params (syncs filter state to/from URL)
    - Reset button ↔ search.setFilters({}) (clears all)
---

<objective>
Replace the vertical FilterSidebar with a horizontal sticky filter bar of dropdown popovers. Each dropdown supports include/exclude toggle with document counts. Filter state syncs to URL params.

Purpose: Horizontal filter bar is the paperless-ngx pattern — more screen-efficient, discoverable, and supports bookmarkable/shareable URLs.
Output: FilterBar, FilterDropdown, FilterSelectionModel, DateRangeFilter components.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/components/FilterSidebar.tsx
@frontend/src/components/FilterChips.tsx
@frontend/src/lib/types.ts
@frontend/src/hooks/useSearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterSelectionModel + FilterDropdown</name>
  <files>
    frontend/src/components/filters/FilterSelectionModel.ts
    frontend/src/components/filters/FilterDropdown.tsx
  </files>
  <action>
    **FilterSelectionModel** (class or utility):
    - Tracks per-item state: 'included' | 'excluded' | 'none'
    - toggle(itemId): none → included → excluded → none (3-state cycle)
    - getIncluded(): string[] — items in 'included' state
    - getExcluded(): string[] — items in 'excluded' state
    - clear(): reset all to 'none'
    - hasSelections(): boolean — any item not 'none'
    - count(): number of non-none items

    **FilterDropdown** (reusable for all facet types):
    Props: title, icon, items: {value: string, label: string, count: number}[], selections, onSelectionChange

    Renders as shadcn Popover:
    - Trigger button: icon + title + count badge (when selections active)
      - Style: outline when no selections, filled/accent when active
    - Popover content:
      - Text input for filtering the list (client-side search)
      - Scrollable list of items (max-h-64):
        - Each item: checkbox/icon (✓ included, ✕ excluded, empty none) + label + count badge
        - Click toggles through 3 states
      - Footer: "Clear" button + "Apply" button
    - Keyboard: Escape closes, Enter applies

    Use shadcn Popover + Command components as building blocks.
  </action>
  <verify>
    FilterDropdown renders a popover with searchable list.
    Clicking items cycles through included/excluded/none states.
    Count badge shows on trigger button when items selected.
  </verify>
  <done>
    FilterDropdown renders correctly with 3-state toggle.
    FilterSelectionModel tracks state cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: FilterBar + URL sync + replace FilterSidebar</name>
  <files>
    frontend/src/components/filters/FilterBar.tsx
    frontend/src/components/filters/DateRangeFilter.tsx
    frontend/src/components/FilterChips.tsx
    frontend/src/App.tsx
  </files>
  <action>
    **FilterBar** (sticky horizontal bar):
    - Position: sticky, top offset below navbar (top-14), z-30, bg-zinc-950 border-b
    - Contains a row of FilterDropdown instances:
      1. Categories (FolderOpen icon) — from facets.categories
      2. Document Type (FileType icon) — from facets.doc_types
      3. Year (Calendar icon) — from facets.years
      4. File Type (File icon) — from facets.file_types
      5. Status (CircleDot icon) — from facets.statuses
      6. Date Range (CalendarRange icon) — custom DateRangeFilter
    - Right end: "Reset filters" button (disabled when no filters active)
    - On filter change: convert FilterSelectionModel states to SearchFilters and call search.setFilters()

    **DateRangeFilter** (special dropdown):
    - Two date inputs: "From" and "To" for created_after / created_before
    - Preset buttons: "This month", "Last 3 months", "This year", "Last year"
    - Apply/Clear buttons

    **URL sync**:
    - On filter change: update URL search params (useSearchParams from react-router)
      e.g., ?q=zahnrad&categories=FEM,Kegelrad&doc_type=Fachbuch&year=2020
    - On page load: read URL params and initialize filters from them
    - This makes filtered views bookmarkable and shareable

    **FilterChips**: Keep but simplify — reads from the same filter state.
    Show active filters as removable chips between FilterBar and results.

    **Migration**: Remove FilterSidebar from the documents page layout.
    The left sidebar in the app shell is now only for navigation (saved views, recent docs).
    Filters live in the sticky FilterBar above results.
  </action>
  <verify>
    FilterBar renders sticky below navbar with all facet dropdowns.
    Selecting a filter updates results.
    URL params reflect current filters.
    Loading a URL with filter params restores the filter state.
    FilterSidebar is removed from documents layout.
  </verify>
  <done>
    Horizontal filter bar replaces vertical sidebar filters.
    Filter state syncs to URL params.
    All existing filter functionality preserved.
  </done>
</task>

</tasks>

<verification>
- [ ] Sticky filter bar renders below navbar with dropdown buttons for each facet
- [ ] Each dropdown shows searchable list with document counts per option
- [ ] Include/exclude toggle works (3-state: ✓/✕/empty)
- [ ] Active selections show count badge on dropdown button
- [ ] "Reset filters" clears all filters
- [ ] Date range filter with presets works
- [ ] URL params reflect current filter state
- [ ] Loading URL with params restores filters
- [ ] FilterChips still show active filters as removable pills
- [ ] FilterSidebar removed — left sidebar is navigation only
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Horizontal filter bar with faceted dropdowns replaces vertical sidebar.
Include/exclude filter logic works.
Filter state syncs to URL query params.
</success_criteria>

<output>
After completion, create `.planning/phases/10-document-list/10-02-SUMMARY.md`
</output>
