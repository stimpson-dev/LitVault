---
phase: 10-document-list
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - frontend/src/components/documents/BulkEditor.tsx
  - frontend/src/components/filters/FilterBar.tsx
  - frontend/src/components/documents/DocumentToolbar.tsx
  - frontend/src/components/documents/LargeCard.tsx
  - frontend/src/components/GridCard.tsx
  - frontend/src/components/ResultRow.tsx
autonomous: true

must_haves:
  truths:
    - When documents are selected, filter bar swaps to bulk action bar
    - Bulk actions include: assign tags, change doc_type, exclude, export
    - Clicking a metadata badge on any card adds it as a filter (click-to-filter)
    - Deselecting all documents restores the filter bar
  artifacts:
    - frontend/src/components/documents/BulkEditor.tsx
  key_links:
    - BulkEditor visibility ↔ selectedIds.size > 0
    - BulkEditor ↔ batch API endpoints (excludeBatch, future tag/type batch)
    - Click-to-filter ↔ search.setFilters (adds filter for clicked value)
    - FilterBar ↔ BulkEditor swap (same sticky position)
---

<objective>
Add bulk editing bar that replaces the filter bar when documents are selected, and click-to-filter on all metadata badges throughout the document list.

Purpose: Bulk operations streamline management; click-to-filter enables zero-cost discovery.
Output: BulkEditor component, click-to-filter on all card metadata.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-document-list/10-01-SUMMARY.md
@.planning/phases/10-document-list/10-02-SUMMARY.md
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BulkEditor bar</name>
  <files>
    frontend/src/components/documents/BulkEditor.tsx
    frontend/src/components/filters/FilterBar.tsx
  </files>
  <action>
    **BulkEditor** component (same dimensions as FilterBar):
    - Left side: "{N} selected" count badge + "Deselect all" button
    - Action buttons:
      - "Exclude" (Archive icon) — calls excludeBatch API, red/destructive style
      - "Export" (Download icon) — exports selected as CSV
      - "Classify" (Sparkles icon) — triggers batch AI classification
      - "Change Type" (FileType icon) — dropdown to assign doc_type to all selected
      - "Add Tags" (Tag icon) — dropdown to add tags to all selected (future API)
    - Right side: count indicator "N of M documents"

    **Swap logic in FilterBar parent**:
    - Wrap FilterBar and BulkEditor in a container div with same sticky positioning
    - When selectedIds.size > 0: render BulkEditor, hide FilterBar
    - When selectedIds.size === 0: render FilterBar, hide BulkEditor
    - Use same height for both so layout doesn't jump

    Existing bulk operations (excludeBatch) already work.
    For new operations (batch tag, batch type change): add placeholder buttons that show
    "Coming soon" toast — backend endpoints can be added in a later phase.
  </action>
  <verify>
    Selecting documents shows bulk editor bar.
    Deselecting all restores filter bar.
    Exclude action works for selected documents.
    Layout doesn't jump during swap.
  </verify>
  <done>Bulk editor bar swaps with filter bar when documents are selected.</done>
</task>

<task type="auto">
  <name>Task 2: Click-to-filter on metadata badges</name>
  <files>
    frontend/src/components/documents/LargeCard.tsx
    frontend/src/components/GridCard.tsx
    frontend/src/components/ResultRow.tsx
  </files>
  <action>
    Make every metadata badge/pill clickable to add a filter:

    **Clickable elements** (all card types):
    - doc_type badge → adds doc_type filter
    - Category badge → adds category filter
    - Author text → adds author filter (future: text filter)
    - Year → adds year filter
    - Tag pill → adds tag filter (as text in query or tag-specific filter)
    - File type icon/label → adds file_type filter

    **Visual pattern**:
    - cursor: pointer on hoverable metadata
    - Subtle hover effect: bg-zinc-800 rounded transition
    - Optional: small Filter icon appears on hover next to the text
    - onClick: call onFilterAdd(filterType, value) prop
    - Parent (DocumentsPage) handles: search.setFilters({...current, [type]: value})

    **Avoid**: Don't make non-filterable text clickable (summary, snippet, title).
    Title click should still open the document detail.

    Add a shared MetadataBadge component if there's enough duplication across card types:
    ```tsx
    <MetadataBadge
      type="doc_type"
      value={doc.doc_type}
      onClick={(type, val) => onFilterAdd(type, val)}
    />
    ```
  </action>
  <verify>
    Clicking a doc_type badge on a card adds it as an active filter.
    Clicking a tag pill on a card adds tag filter.
    Results update to show only matching documents.
    FilterChips show the new filter as a removable chip.
  </verify>
  <done>
    All metadata badges are clickable and add corresponding filters.
    Click-to-filter works on all 3 display modes (table, grid, large).
  </done>
</task>

</tasks>

<verification>
- [ ] Selecting documents swaps filter bar → bulk editor
- [ ] Deselecting restores filter bar (no layout jump)
- [ ] Bulk exclude action works
- [ ] Clicking doc_type badge filters results
- [ ] Clicking tag pill filters results
- [ ] Clicking year filters results
- [ ] FilterChips reflect click-to-filter additions
- [ ] Click-to-filter works in all 3 display modes
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Bulk editor swaps with filter bar seamlessly.
Click-to-filter works on all metadata badges in all display modes.
</success_criteria>

<output>
After completion, create `.planning/phases/10-document-list/10-03-SUMMARY.md`
</output>
