# Plan 08-01: Echtzeit-Suche mit Prefix-Matching

## Goal
Suche reagiert live während der Eingabe — "Zahnr" findet bereits "Zahnrad".

## Current Problem
`SearchService.search()` nutzt `sanitize_fts5_query()` (exakte Wörter), nicht `sanitize_fts5_query_with_prefix()` (Prefix-Match mit `*`). FTS5 findet nur komplette Wörter.

## Tasks

### T1: Backend — Prefix-Matching aktivieren
**File**: `backend/app/search/service.py`
- Zeile 44: `sanitize_fts5_query` → `sanitize_fts5_query_with_prefix`
- Import anpassen

### T2: Leere Suche zeigt ALLE Dokumente (nicht nur "done")
**File**: `backend/app/search/service.py`
- Zeile 113: `WHERE d.processing_status = 'done'` → `WHERE 1=1` (oder processing_status-Filter als Option)
- Damit auch Dokumente mit Fehlern sichtbar sind

## Verification
- [ ] Tippe "Zahnr" → Ergebnisse mit "Zahnrad" erscheinen
- [ ] Tippe "FVA" → alle FVA-Dokumente erscheinen während des Tippens
- [ ] Leere Suche zeigt alle Dokumente
