---
phase: 06-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/FilterSidebar.tsx
  - frontend/src/components/FilterChips.tsx
autonomous: true

must_haves:
  truths:
    - Left sidebar shows filter facets grouped by category, doc_type, year
    - Each facet shows count badge
    - Clicking a facet applies the filter
    - Active filters shown as removable chips above results
    - Clear-all button removes all filters
    - Facet counts update when filters change
  artifacts:
    - frontend/src/components/FilterSidebar.tsx
    - frontend/src/components/FilterChips.tsx
  key_links:
    - FilterSidebar reads facets from search results
    - FilterSidebar calls setFilters to apply filter
    - FilterChips shows active filters with remove buttons
---

<objective>
Build the filter sidebar with facet counts and active filter chips.

Purpose: Enable drill-down filtering by category, document type, and year range.
Output: FilterSidebar with clickable facets, FilterChips with clear-all.
</objective>

<execution_context>
@skills/gsd/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/lib/types.ts
@frontend/src/hooks/useSearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Filter sidebar with facet counts</name>
  <files>frontend/src/components/FilterSidebar.tsx</files>
  <action>
  Create `frontend/src/components/FilterSidebar.tsx`:

  Props:
  - facets: SearchFacets (from search results)
  - filters: SearchFilters (current active filters)
  - onFilterChange: (filters: SearchFilters) => void

  Structure:
  1. **Categories section**:
     - Heading: "Kategorien"
     - List of facets.categories, each showing name + count badge
     - Clicking a category sets filters.category = name (or clears if already selected)
     - Selected category highlighted (brighter bg)

  2. **Document Types section**:
     - Heading: "Dokumenttyp"
     - List of facets.doc_types
     - Clicking sets filters.doc_type
     - Show localized labels for types (dissertation → "Dissertation", paper → "Paper", etc.)

  3. **Years section**:
     - Heading: "Jahr"
     - List of facets.years (show as clickable items)
     - Clicking a year sets both year_min and year_max to that year
     - Or show as a simple scrollable list

  4. **Language section** (optional, if data available):
     - Small section showing detected languages

  Style:
  - Width: w-64 or w-72
  - Dark theme: zinc-900 bg, zinc-400 text, zinc-700 borders
  - Scrollable if content overflows (overflow-y-auto)
  - Count badges: zinc-700 bg, small rounded pill
  - Selected items: zinc-700 bg with white text
  - Sections separated by dividers
  - Collapsible sections (optional, simple show/hide)
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -10` — no type errors in FilterSidebar
  </verify>
  <done>
  FilterSidebar with category, doc_type, year facets. Clickable items with count badges. Selected state highlighted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Active filter chips with clear-all</name>
  <files>frontend/src/components/FilterChips.tsx</files>
  <action>
  Create `frontend/src/components/FilterChips.tsx`:

  Props:
  - filters: SearchFilters
  - onFilterChange: (filters: SearchFilters) => void

  Renders a horizontal row of chips for each active filter:
  - Category chip: shows category name with X button to remove
  - Doc type chip: shows doc type with X button
  - Year chip: shows year or year range with X button
  - Language chip: shows language with X button
  - Author chip: shows author with X button

  Each chip:
  - Small rounded pill
  - Label + X icon (lucide X)
  - Clicking X removes that specific filter (sets to undefined)
  - Style: zinc-700 bg, zinc-200 text, hover:zinc-600

  Clear-all button:
  - Only shown when at least one filter is active
  - "Alle Filter löschen" text
  - Clicking resets all filters to {}
  - Style: text-zinc-400, hover:text-zinc-200

  If no filters active: render nothing (return null)
  </action>
  <verify>
  `cd frontend && npx tsc --noEmit 2>&1 | head -10` — no type errors
  </verify>
  <done>
  FilterChips shows active filters as removable chips. Clear-all button. Hidden when no filters active.
  </done>
</task>

</tasks>

<verification>
```bash
cd frontend && npm run build
```
Build must succeed.
</verification>

<success_criteria>
- FilterSidebar: categories, doc_types, years with counts and click handlers
- FilterChips: active filter pills with remove, clear-all
- Type-safe props matching useSearch types
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend/06-02-SUMMARY.md`
</output>
