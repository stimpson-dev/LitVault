---
phase: 11-document-detail
plan: 01
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - frontend/src/components/detail/DocumentDetailPage.tsx
  - frontend/src/components/detail/DetailTabs.tsx
  - frontend/src/components/detail/DetailsTab.tsx
  - frontend/src/components/detail/ContentTab.tsx
  - frontend/src/components/detail/MetadataTab.tsx
  - frontend/src/components/detail/NotesTab.tsx
  - frontend/src/components/DocumentDetail.tsx
  - frontend/src/lib/router.tsx
  - frontend/package.json
autonomous: true

must_haves:
  truths:
    - Clicking a document navigates to /documents/:id full-page detail view
    - Left panel (40%) shows tabbed form with Details, Content, Metadata, Notes
    - Right panel (60%) shows sticky PDF viewer rendering the document
    - On mobile (<768px), PDF viewer becomes a tab instead of side panel
    - Back button returns to the document list with filters preserved
  artifacts:
    - frontend/src/components/detail/DocumentDetailPage.tsx
    - frontend/src/components/detail/DetailTabs.tsx
    - frontend/src/components/detail/DetailsTab.tsx
    - frontend/src/components/detail/PdfViewer.tsx
  key_links:
    - DocumentDetailPage ↔ router param :docId (loads document by ID)
    - PdfViewer ↔ document file_path (renders PDF from backend)
    - DetailsTab ↔ PATCH /api/documents/:id (inline edit saves)
    - Back navigation ↔ preserved URL params (filters in URL)
---

<objective>
Redesign the document detail from a 400px side panel to a full-page split-pane view with tabbed metadata form on the left and sticky PDF viewer on the right.

Purpose: Split-pane detail is the paperless-ngx pattern — gives documents the space they deserve for reading + editing.
Output: DocumentDetailPage with tabs + PDF viewer, wired to /documents/:id route.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/components/DocumentDetail.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocumentDetailPage split-pane + route</name>
  <files>
    frontend/src/components/detail/DocumentDetailPage.tsx
    frontend/src/components/detail/DetailTabs.tsx
    frontend/src/components/detail/DetailsTab.tsx
    frontend/src/components/detail/ContentTab.tsx
    frontend/src/components/detail/MetadataTab.tsx
    frontend/src/lib/router.tsx
  </files>
  <action>
    **DocumentDetailPage** (full-page, renders in AppShell outlet):
    - Fetches document by ID from route param (useParams)
    - Split-pane layout:
      ```
      <div className="flex h-[calc(100vh-3.5rem)]">
        <div className="w-2/5 overflow-y-auto border-r border-zinc-800 p-6">
          <DetailTabs doc={document} onSave={handleSave} />
        </div>
        <div className="w-3/5 sticky top-14 h-[calc(100vh-3.5rem)]">
          <PdfViewer filePath={document.file_path} /> {/* Task 2 */}
        </div>
      </div>
      ```
    - Top bar: Back button (ArrowLeft, navigates to previous page preserving URL params),
      document title, action buttons (Classify, Rescan, Open File, Exclude)
    - Mobile (<768px): hide right panel, add "Preview" tab to DetailTabs

    **DetailTabs** (tab container using shadcn Tabs or custom):
    - Tab 1 "Details": DetailsTab — inline-editable metadata form
    - Tab 2 "Content": ContentTab — full text in scrollable textarea
    - Tab 3 "Metadata": MetadataTab — read-only file info
    - Tab 4 "Notes": NotesTab — future, placeholder for now
    - Tab 5 "Preview": only on mobile, renders PdfViewer
    - Active tab reflected in URL: /documents/:id/:tab (optional)

    **DetailsTab** (port from existing DocumentDetail):
    - Inline-editable fields: title, authors, year, doc_type (dropdown), source, language, summary
    - Tags section with TagEditor
    - Classification info: source badge, confidence bar
    - Favorite toggle button
    - Save button at bottom (or auto-save on blur)

    **ContentTab**: Read-only textarea showing document.full_text (scrollable, monospace)
    **MetadataTab**: Read-only grid: file_path, file_type, file_size, page_count, sha256_hash,
      processing_status, created_at, updated_at, classification_source, confidence

    **Route**: Wire /documents/:id in router.tsx → DocumentDetailPage
    Keep the old DocumentDetail side panel as a quick-preview option (right-click → preview).
  </action>
  <verify>
    Navigating to /documents/1 shows split-pane detail page.
    Left panel shows tabs, right panel shows viewer placeholder.
    Back button returns to document list.
    Inline editing saves via PATCH API.
    All metadata fields render correctly.
  </verify>
  <done>
    Full-page document detail with tabbed form renders on /documents/:id.
    Inline editing works. Back navigation preserves filters.
  </done>
</task>

<task type="auto">
  <name>Task 2: PDF Viewer integration</name>
  <files>
    frontend/src/components/detail/PdfViewer.tsx
    frontend/package.json
    backend/app/documents/router.py
  </files>
  <action>
    Install react-pdf (or @react-pdf-viewer/core) for PDF rendering.

    **Backend**: Add endpoint GET /api/documents/:id/file that serves the actual file
    (streams file from file_path). Set Content-Type based on file extension.
    Add Content-Disposition: inline header for browser rendering.
    Handle missing files gracefully (404).

    **PdfViewer component**:
    - Renders PDF from /api/documents/:id/file URL
    - Controls: page navigation (prev/next + page number input), zoom (fit width / fit page / percentage)
    - Loading state: skeleton placeholder while PDF loads
    - Error state: "Could not load preview" with file info
    - For non-PDF files (DOCX, PPTX): show file type icon + "Preview not available" + Download button
    - For images (if supported later): direct <img> render
    - Sticky positioning: stays in view while left panel scrolls
    - Dark background matching the app theme

    **Fallback**: If react-pdf has issues with certain PDFs, offer an "Open in system viewer"
    button that calls window.open() with the file URL.
  </action>
  <verify>
    PDF renders in the right panel of document detail.
    Page navigation works.
    Zoom controls work.
    Non-PDF files show appropriate fallback.
    Backend serves files with correct content type.
  </verify>
  <done>
    PDF viewer renders documents inline.
    Page navigation and zoom work.
    Backend serves document files via API.
  </done>
</task>

</tasks>

<verification>
- [ ] /documents/:id renders split-pane detail page
- [ ] Left panel: tabs (Details, Content, Metadata, Notes) all render
- [ ] Right panel: PDF viewer shows document
- [ ] Inline editing works on all editable fields
- [ ] PDF page navigation and zoom work
- [ ] Non-PDF files show fallback message
- [ ] Back button returns to document list with filters preserved
- [ ] Mobile: preview is a tab, not a side panel
- [ ] Backend /api/documents/:id/file serves files correctly
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Full-page split-pane document detail with PDF viewer works.
All metadata editable inline with save.
Navigation between list and detail preserves state.
</success_criteria>

<output>
After completion, create `.planning/phases/11-document-detail/11-01-SUMMARY.md`
</output>
