---
phase: 09-app-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/AppSidebar.tsx
  - frontend/src/components/layout/AppNavbar.tsx
  - frontend/src/components/layout/SidebarSection.tsx
  - frontend/src/lib/router.tsx
  - frontend/package.json
autonomous: true

must_haves:
  truths:
    - App renders with a persistent sidebar + top navbar that survive page navigation
    - Sidebar collapses to 50px icon rail on toggle, expands to ~256px full width
    - Sidebar preference persists across page reloads (stored in settings)
    - On mobile (<768px), sidebar is a full-width drawer toggled by hamburger
    - Main content area fills remaining space and scrolls independently
  artifacts:
    - frontend/src/components/layout/AppShell.tsx
    - frontend/src/components/layout/AppSidebar.tsx
    - frontend/src/components/layout/AppNavbar.tsx
    - frontend/src/lib/router.tsx
  key_links:
    - Sidebar slim state ↔ useSettings hook (persists to config.json)
    - Router outlet ↔ content area (pages render inside shell)
    - Navbar search ↔ search state (SearchBar moves into navbar)
---

<objective>
Replace the current flat single-page layout with a proper app shell: persistent sidebar + top navbar + routed content area. Install react-router-dom for client-side routing. The sidebar supports slim mode (50px icon rail ↔ full width).

Purpose: Foundation for all subsequent UI phases — every page renders inside this shell.
Output: AppShell, AppSidebar, AppNavbar components + router setup.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/App.tsx
@frontend/src/hooks/useSettings.ts
@frontend/src/components/SearchBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom + set up routes</name>
  <files>
    frontend/package.json
    frontend/src/lib/router.tsx
    frontend/src/main.tsx
  </files>
  <action>
    Install react-router-dom v7. Create router.tsx with BrowserRouter wrapping the app.
    Define routes:
    - "/" → DocumentsPage (the current search/results view)
    - "/view/:id" → DocumentsPage with saved view loaded
    - "/documents/:docId" → DocumentDetailPage (full-page detail, Phase 11)
    - "/dashboard" → DashboardPage (Phase 12, placeholder for now)
    - "/settings" → SettingsPage (placeholder, still modal for now)
    - "/review" → ReviewQueuePage (placeholder)

    For now, all routes render placeholder components that just show their name.
    Wrap the app in BrowserRouter in main.tsx.
  </action>
  <verify>npm run build succeeds. Navigating to / and /dashboard shows different content.</verify>
  <done>Routes defined and rendering. BrowserRouter wraps the app.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell + AppNavbar + AppSidebar layout</name>
  <files>
    frontend/src/components/layout/AppShell.tsx
    frontend/src/components/layout/AppSidebar.tsx
    frontend/src/components/layout/AppNavbar.tsx
    frontend/src/components/layout/SidebarSection.tsx
    frontend/src/App.tsx
  </files>
  <action>
    Create the persistent app shell layout:

    **AppNavbar** (top, fixed, h-14):
    - Left: hamburger toggle (mobile) / slim toggle button
    - Center: SearchBar component (moved from current header)
    - Right: ThemeToggle + settings gear icon

    **AppSidebar** (left, sticky, height calc(100vh - 56px)):
    - CSS variable `--sidebar-width` toggled between 256px and 50px
    - Transition: width 0.2s ease, text opacity 0.1s ease (text fades first)
    - State: `sidebarSlim` boolean in useSettings or localStorage
    - Mobile: position fixed, full width, hidden by default, shown on hamburger

    Sections (using SidebarSection component for grouping):
    1. **Main**: Dashboard (LayoutDashboard icon), Documents (FileText icon)
    2. **Saved Views**: placeholder — "No saved views" text (Phase 10 populates this)
    3. **Manage**: Tags (Tag icon), Settings (Settings icon) — link to modals/pages
    4. **Footer**: LitVault version, slim mode toggle button at bottom

    Each nav item: icon always visible, text label hidden when slim (opacity 0 → display none).
    Active route highlighted with accent background.

    **AppShell** wraps everything:
    ```
    <AppNavbar />
    <div className="flex pt-14"> {/* navbar height offset */}
      <AppSidebar />
      <main style={{ marginLeft: 'var(--sidebar-width)' }}>
        <Outlet /> {/* react-router */}
      </main>
    </div>
    ```

    **App.tsx**: Refactor to be just the router provider + LanguageProvider + AppShell wrapper.
    Move current search/results/detail layout into a new DocumentsPage component.
  </action>
  <verify>
    npm run build succeeds.
    Sidebar renders with icons and labels.
    Clicking slim toggle collapses sidebar to icon-only rail.
    SearchBar works in navbar.
    Current search/results functionality intact on "/" route.
  </verify>
  <done>
    App shell renders with persistent sidebar + navbar.
    Sidebar toggles between full (256px) and slim (50px) modes.
    Content area adjusts width on sidebar toggle.
    All existing functionality preserved on the documents route.
  </done>
</task>

</tasks>

<verification>
- [ ] App renders with navbar + sidebar + content area
- [ ] Sidebar slim toggle works (256px ↔ 50px with smooth animation)
- [ ] Mobile hamburger shows/hides sidebar drawer
- [ ] SearchBar is in the navbar and still functions (debounced search)
- [ ] "/" route shows existing document search/results/detail view
- [ ] "/dashboard" route shows placeholder
- [ ] Sidebar nav items highlight active route
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Persistent app shell with collapsible sidebar renders on all routes.
Existing search functionality is preserved.
Router navigates between at least 2 routes without full page reload.
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-shell/09-01-SUMMARY.md`
</output>
