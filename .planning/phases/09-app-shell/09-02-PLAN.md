---
phase: 09-app-shell
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/src/components/layout/AppSidebar.tsx
  - frontend/src/components/layout/SavedViewNav.tsx
  - frontend/src/components/layout/RecentDocuments.tsx
  - frontend/src/hooks/useRecentDocs.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/types.ts
autonomous: true

must_haves:
  truths:
    - Saved searches appear as clickable nav items in the sidebar
    - Clicking a saved view navigates to /view/:id and loads its filters
    - Recently opened documents appear in sidebar with title + close button
    - Sidebar sections are collapsible (click header to collapse/expand)
  artifacts:
    - frontend/src/components/layout/SavedViewNav.tsx
    - frontend/src/components/layout/RecentDocuments.tsx
    - frontend/src/hooks/useRecentDocs.ts
  key_links:
    - SavedViewNav ↔ saved searches API (fetches list on mount)
    - SavedViewNav click ↔ router navigation to /view/:id
    - RecentDocuments ↔ useRecentDocs hook (sessionStorage-backed)
    - Sidebar sections ↔ collapsible state (localStorage per section)
---

<objective>
Populate the sidebar with saved views as first-class navigation items and a "recently opened" documents tracker. Saved views become the primary way to access filtered document sets.

Purpose: Elevates saved searches from a hidden modal to the main navigation — the single biggest UX improvement from paperless-ngx.
Output: SavedViewNav, RecentDocuments components + useRecentDocs hook.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-app-shell/09-01-SUMMARY.md
@frontend/src/components/SavedSearches.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Saved views as sidebar navigation</name>
  <files>
    frontend/src/components/layout/SavedViewNav.tsx
    frontend/src/components/layout/AppSidebar.tsx
    frontend/src/lib/router.tsx
  </files>
  <action>
    Create SavedViewNav component:
    - Fetches saved searches from GET /api/search/saved on mount
    - Renders each as a nav item: Bookmark icon + name
    - Click navigates to /view/{id} route
    - Active view highlighted (matches current route param)
    - In slim mode: show only Bookmark icon, popover with name on hover
    - "Create view" button at bottom (opens saved search creation)
    - Badge showing document count per saved view (optional, fetch on hover or periodically)

    Wire up /view/:id route in router.tsx:
    - Load the saved search by ID from API
    - Apply its query + filters to the search state
    - Render the DocumentsPage with those filters active

    Add to AppSidebar between "Main" and "Manage" sections.
  </action>
  <verify>
    Saved views appear in sidebar.
    Clicking one navigates to /view/:id and loads the saved filters.
    Active view is visually highlighted.
  </verify>
  <done>Saved views render as sidebar nav items and navigate to filtered document lists.</done>
</task>

<task type="auto">
  <name>Task 2: Recently opened documents + collapsible sections</name>
  <files>
    frontend/src/components/layout/RecentDocuments.tsx
    frontend/src/hooks/useRecentDocs.ts
    frontend/src/components/layout/SidebarSection.tsx
    frontend/src/components/layout/AppSidebar.tsx
  </files>
  <action>
    **useRecentDocs hook**:
    - Stores last 10 opened document IDs + titles in sessionStorage
    - addRecent(id, title) — adds to front, deduplicates
    - removeRecent(id) — removes entry
    - clearAll() — clears list
    - Returns: recentDocs: {id, title}[], addRecent, removeRecent, clearAll

    **RecentDocuments component**:
    - Lists recent docs: truncated title + X close button
    - Click navigates to document detail (or selects it in the current view)
    - "Close all" link at bottom when >1 item
    - In slim mode: show FileText icon only, popover with list on hover
    - Empty state: "No recent documents" in muted text

    **SidebarSection** — make sections collapsible:
    - Click section header to toggle collapse (ChevronDown/ChevronRight icon)
    - Collapse state stored in localStorage keyed by section name
    - Smooth height animation (max-height transition or CSS grid trick)
    - In slim mode: sections always expanded (no headers shown)

    Add RecentDocuments section to AppSidebar between Saved Views and Manage.
    Call addRecent() whenever a document is opened in DocumentDetail.
  </action>
  <verify>
    Opening a document adds it to "Recent" in sidebar.
    Clicking recent doc opens it.
    X button removes from list.
    Section headers collapse/expand their content.
  </verify>
  <done>
    Recently opened documents tracked in sidebar.
    All sidebar sections are collapsible with persisted state.
  </done>
</task>

</tasks>

<verification>
- [ ] Saved views appear as nav items in sidebar
- [ ] Clicking a saved view loads its filters on the documents page
- [ ] Active saved view is visually highlighted
- [ ] Recent documents appear in sidebar after opening a document
- [ ] Close button removes a recent document entry
- [ ] Sidebar sections collapse/expand on header click
- [ ] Collapse state persists across page refresh
- [ ] Slim mode: icons only, popovers on hover
- [ ] npm run build produces no errors
</verification>

<success_criteria>
Saved views are first-class sidebar navigation items.
Recently opened documents are tracked and accessible from sidebar.
All sidebar sections are collapsible with persisted state.
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-shell/09-02-SUMMARY.md`
</output>
